usage: |
  基于 ffmpeg.wasm 的浏览器本地视频处理工具，NAS 仅托管静态页面，转码算力在用户浏览器本地执行。
  首次使用需联网下载 ffmpeg.wasm（约 31MB）；PWA 缓存后可离线使用。
  建议单文件 ≤500MB；超大/4K 可能因浏览器内存不足失败。

application:
  subdomain: ffmpeg
  routes:
    - /=http://web:8080

services:
  web:
    image: embed:ffmpeg-webcli
